#include <stdio.h>

struct Agenda{
    char nome[30];
    int idade;
}
typedef struct Agenda Agenda;

void Insert(); //To Do
void Remove(); //To Do
void List(); //To Do

int main(){ //On Going

void *pBuffer;
pBuffer = malloc(2);

free(pBuffer);
return 0;
}
